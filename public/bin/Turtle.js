// Generated by CoffeeScript 1.10.0
(function() {
  window.Turtle = (function() {
    function Turtle(position, angle) {
      this.position = position != null ? position : {
        x: 0,
        y: 0
      };
      this.angle = angle != null ? angle : 0;
    }

    Turtle.prototype.move = function(distance) {
      var radians;
      radians = Math.PI * this.angle / 180;
      this.position.x = this.position.x + (distance * Math.cos(radians));
      this.position.y = this.position.y + (distance * Math.sin(radians));
      return this;
    };

    Turtle.prototype.rotate = function(degree) {
      this.angle += degree;
      return this;
    };

    return Turtle;

  })();

}).call(this);
